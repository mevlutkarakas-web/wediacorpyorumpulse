import { Bot,CheckCircle2,KeyRound,Youtube } from "lucide-react";
export default function SettingsPage(){const integrations=[{Icon:Youtube,title:"YouTube Data API v3",key:"YOUTUBE_API_KEY",ready:!!process.env.YOUTUBE_API_KEY},{Icon:Bot,title:"Gemini API",key:"GEMINI_API_KEY",ready:!!process.env.GEMINI_API_KEY}];return <div className="mx-auto max-w-4xl space-y-6"><div><h1 className="text-3xl font-black">Ayarlar</h1><p className="mt-1 text-sm text-slate-500">Entegrasyonların gerçek ortam durumunu görüntüleyin.</p></div>{integrations.map(({Icon,title,key,ready})=><section className="card p-6" key={key}><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-violet-50 text-violet-600 dark:bg-violet-500/10"><Icon/></span><div><h2 className="font-bold">{title}</h2><p className="text-xs text-slate-500">{key} ortam değişkeninden okunur</p></div><span className={`tag ml-auto ${ready?"bg-emerald-50 text-emerald-600":"bg-amber-50 text-amber-600"}`}>{ready?<><CheckCircle2 size={12} className="mr-1"/>Yapılandırıldı</>:<><KeyRound size={12} className="mr-1"/>Anahtar gerekli</>}</span></div>{!ready&&<p className="mt-5 rounded-xl bg-muted p-4 text-sm text-slate-500"><code>{key}</code> değerini <code>.env</code> dosyasına ekleyip geliştirme sunucusunu yeniden başlatın.</p>}</section>)}</div>}
